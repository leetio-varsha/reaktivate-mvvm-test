{
  "name": "reaktivate-tdd-challenge-1-sample-begin",
  "version": "1.0.0",
  "description": "How we create a fast-test architecture from scratch.",
  "keywords": [],
  "main": "src/index.ts",
  "homepage": "https://leetio-varsha.github.io/reaktivate-mvvm-test",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "cross-env NODE_OPTIONS=--openssl-legacy-provider react-scripts start",
    "build": "cross-env NODE_OPTIONS=--openssl-legacy-provider react-scripts build",
    "test": "cross-env NODE_OPTIONS=--openssl-legacy-provider react-scripts test",
    "eject": "cross-env NODE_OPTIONS=--openssl-legacy-provider react-scripts eject"
  },
  "dependencies": {
    "mobx": "6.3.10",
    "mobx-react": "7.2.1",
    "react": "16.8.6",
    "react-dom": "16.8.6",
    "react-scripts": "3.0.1"
  },
  "devDependencies": {
    "@types/babel__traverse": "7.0.7",
    "@types/jest": "^29.5.14",
    "cross-env": "^7.0.3",
    "gh-pages": "^6.3.0",
    "prettier": "^3.5.2",
    "typescript": "^5.7.3"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
